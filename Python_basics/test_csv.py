import pandas as pd
import os
import datetime

print(datetime.date.today().isoformat())
# result_dataset = []
# result_dataset.append(
#                 {
#                     "name": "Nicole",
#                     "second_name": "Colgan"
#                 }
#             )
# df = pd.DataFrame(result_dataset) 
# output_path = os.path.join(os.path.dirname(__file__),"..", "output2", "out_out", "out.csv")
# df.to_csv(output_path, index=False)

#remember the user (me) is currently passing in a file with a file ending so lets see what it does first